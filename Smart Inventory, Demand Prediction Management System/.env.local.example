# Environment Variables for Smart Inventory Management System
# Copy this file to .env.local and fill in your actual values

# MS SQL Server Database Configuration
# ====================================

# Database server address (use localhost for local development)
DB_SERVER=localhost

# Database name
DB_DATABASE=SmartInventoryDB

# Database username
DB_USER=sa

# Database password (replace with your actual password)
DB_PASSWORD=your_password_here

# Database port (default SQL Server port is 1433)
DB_PORT=1433

# Trust server certificate (set to true for local development)
DB_TRUST_SERVER_CERTIFICATE=true
